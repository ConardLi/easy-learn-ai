import type { Narration } from "../../registry/types";

/**
 * Chapter 13 · compaction-problem — 压缩的麻烦（5 steps · ~44s）
 *
 * Source: script.md beats — from "最后一条，关于上下文压缩"
 *                          to "对话越长越贵"
 */
export const narrations: Narration[] = [
  // step 0 (~6s) — 章节标识：最后一条 / 上下文压缩 / 技术上最巧妙的一条
  "最后一条，关于上下文压缩。也是技术上最巧妙的一条。",
  // step 1 (~10s) — 场景：上下文窗口填满 → 压缩成摘要腾空间
  "长对话跑久了，上下文窗口会被填满。这时候要把之前的对话压缩成摘要，腾出空间继续聊。",
  // step 2 (~14s) — 朴素错误做法：另起请求 + 不同 system_prompt + 没带 tools
  "但问题来了。如果你另起一个请求做压缩，用了不同的系统指令，没带工具定义。那从第一个字开始，就跟主对话的缓存对不上。",
  // step 3 (~3s) — 双链断
  "两条缓存链，互相不复用。",
  // step 4 (~11s) — 成本警告：完整、无缓存折扣 + 对话越长越贵
  "而且你要把整段对话发过去做摘要。这时候你付的是完整的、没有缓存折扣的费用。对话越长越贵。",
];
